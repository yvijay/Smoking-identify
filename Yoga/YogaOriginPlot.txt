% 对输入数据进行平滑处理
smoothedData = smoothdata(Untitled,'movmean','SmoothingFactor',0.3);

% 显示结果
clf
plot(Untitled,'Color',[0 114 189]255,'DisplayName','original data ')
xlabel('Sampling frequency(time)');
ylabel('phase(rad)');
legend